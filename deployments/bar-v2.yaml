apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: bar-v2
spec:
  template:
    metadata:
      labels:
        app: bar
        version: v2
    spec:
      containers:
        - name: backend
          image: gcr.io/hightowerlabs/backend:v2
          imagePullPolicy: Always
          env:
            - name: REGION
              valueFrom:
                configMapKeyRef:
                  name: cluster
                  key: region
          args:
            - "-grpc=0.0.0.0:8000"
            - "-http=0.0.0.0:80"
            - "-region=$(REGION)"
          ports:
            - name: grpc
              containerPort: 8000
            - name: http
              containerPort: 80
          resources:
            limits:
              cpu: 500m
              memory: 20M
            requests:
              cpu: 100m
              memory: 10M
